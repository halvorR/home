// Router

import React, { PropTypes } from 'react'

const Api = React.createClass({
  render () {
    return (
      <div>API for Holter live</div>
        <div>API for Holter live</div>
        
    )
  }
})

export default Api
